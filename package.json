{
  "scripts": {
    "clean:prod": "bun -e \"await Bun.$`rm -rf _site_prod`\"",
    "build": "bun run clean:prod && ELEVENTY_ENV=production eleventy",
    "dev": "ELEVENTY_ENV=development eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "esbuild": "^0.27.3",
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.0.0"
  }
}
